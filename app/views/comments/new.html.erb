<% if user_signed_in? %>

  <%= form_for @comment, url: post_comments_path(@post) do |f| %>
  <%= f.label :content, 'Your Comment' %>
  <%= f.text_area :content %>
  <%= f.submit %>

  <% end %>

<% end %>
