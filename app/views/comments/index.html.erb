<h2> <%= @post.title %> <br>
     <%= @post.content %> <br>
     <%= @post.url %>
</h2>

<ul>
    <% @comments.each do |comment| %>
      <%= comment.content %>
      <%= comment.created_at %>
      <%= comment.user.username %>
    <% end %>
</ul>
